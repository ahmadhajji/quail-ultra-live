# Changelog

## Unreleased

- removed leftover Electron entrypoints, packaging scripts, and desktop release docs
- moved shared browser assets into the React frontend build so the server no longer depends on the legacy `web/` tree
- replaced desktop release CI with a web-focused repository check workflow
